// matter.js
//

var expect = require('chai').expect;
var matter = require('../lib/commands/matter');

describe('The `collider matter [<command>] [--help] [--version] [<args>]` handler', function () {

  describe('The `collider matter` handler', function () {
    it('Shows a list of Matter libraries in the current project', function () {

    });
  });

  describe('The `collider matter clone <url> [<locale>]` handler', function () {
    it('Clones a Matter library into the current project', function () {

    });
  });

  describe('The `collider matter (rm | remove) <locale>` handler', function () {
    it('Removes a Matter library from the current project', function () {

    });
  });
});
