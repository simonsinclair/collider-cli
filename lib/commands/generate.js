// generate.js
//

'use strict';

var createError  = require('../createError');
var logErrorExit = require('../logErrorExit');
var pkg = require('../../package.json');

// GENERATE
//

module.exports = function (argv) {
  console.log('collider: the "generate" command is coming soon!');
};
